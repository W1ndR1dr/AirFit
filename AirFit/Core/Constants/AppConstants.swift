import SwiftUI
import Foundation

struct AppConstants {
    static let appName = "AirFit"
    static let defaultCornerRadius: CGFloat = 12.0
    static let defaultPadding: CGFloat = 16.0
    // Placeholder API URLs
    static let weatherAPIBaseURL = "https://api.exampleweather.com/v1/"
    static let aiRouterAPIBaseURL = "https://api.exampleairouter.com/v1/"
}
